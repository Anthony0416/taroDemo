<block wx:if="{{$taroCompReady}}">
    <view class="index">
        <navigator wx:key="index" url="{{item.path}}" wx:for="{{lists}}" wx:for-item="item" wx:for-index="index"><text class="{{'iconfont ' + item.icon}}"></text><text class="name">{{item.name}}</text>
        </navigator>
    </view>
</block>